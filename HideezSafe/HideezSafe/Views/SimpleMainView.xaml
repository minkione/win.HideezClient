<Metro:MetroWindow
    x:Class="HideezSafe.Views.SimpleMainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Metro="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:controls="clr-namespace:HideezSafe.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:l="clr-namespace:HideezSafe.Modules.Localize"
    xmlns:local="clr-namespace:HideezSafe.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding Version, StringFormat=Hideez Safe ({0})}"
    Width="310"
    Height="250"
    MinWidth="310"
    MinHeight="250"
    Background="{DynamicResource WindowLeftColumBrush}"
    Closing="MetroWindow_Closing"
    DataContext="{Binding Main, Mode=OneWay, Source={StaticResource Locator}}"
    TitleCharacterCasing="Normal"
    Visibility="Hidden"
    WindowTitleBrush="{DynamicResource WindowTitleBrush}"
    mc:Ignorable="d">

    <Grid VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:DevicesExpander VerticalAlignment="Top">
            <controls:DevicesExpander.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Offset="0" Color="#0022446D" />
                    <GradientStop Offset="1" Color="#5C22446D" />
                </LinearGradientBrush>
            </controls:DevicesExpander.Background>
        </controls:DevicesExpander>

        <controls:ConnectivityIndicators
            Grid.Row="1"
            Margin="24"
            Padding="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            Focusable="False" />

        <!--<TextBlock
            Margin="40"
            Padding="5"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Background="Red"
            FontSize="45"
            Text="{Binding DevicesExpander.Devices[0].Proximity, Source={StaticResource Locator}}"
            Visibility="Visible" />-->

    </Grid>
</Metro:MetroWindow>
